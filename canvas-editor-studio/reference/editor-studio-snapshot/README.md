# Reference Prototype Snapshot

This folder contains a sanitized source snapshot captured during early exploration.

- It is kept here only as a reference snapshot.
- It is not part of the runnable project in the repository root.
- The actual standalone demo lives in the project root.
- The code-like files are intentionally renamed with `.txt` suffixes so Vite will not treat this folder as a second app entry.
